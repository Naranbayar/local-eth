<nav class="navbar-sticky-top navbar-expand-lg navbar-light double-nav mobile-nofixed">
    <ul class="navbar-nav">
        <li class="nav-item active">
            <a class="nav-link" href="/">
                <p class="text-black">Locale-Etherium</p>
            </a>
        </li>
        <% if (user==null) { %>
        <li class="nav-item active">
            <a class="nav-link" href="/">Home</a>
        </li>

        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <form class="form-inline mr-sm-2">
                    <% if (user==null) { %>
                    <a href="/login" class="btn btn-info"><span class="fa fa-user"></span>Sign in</a>
                    <a href="/signup" class="btn btn-info"><span class="fa fa-user"></span>Sign up</a>
                    <% } else if (user && (user.email)) { %>
                    <a href="/logout" class="btn btn-info"><span class="fa fa-user"></span>logout</a>
                    <% } %>
                </form>
            </li>
        </ul>
        <% } else if (user && (user.email)) { %>
        <li class="nav-item">
            <a class="nav-link" href="/browse">Browse</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/trades">Trades</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/offers">Offers</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/wallet">Wallet</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="/profile">Profile</a>
        </li>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <form class="form-inline mr-sm-2">
                    <% if (user==null) { %>
                    <a href="/login" class="btn btn-info"><span class="fa fa-user"></span>Sign in</a>
                    <a href="/signup" class="btn btn-info"><span class="fa fa-user"></span>Sign up</a>
                    <% } else if (user && (user.email)) { %>
                    <a href="/logout" class="btn btn-info"><span class="fa fa-user"></span>logout</a>
                    <% } %>
                </form>
            </li>
        </ul>
        <% } %>
    </ul>
</nav>